# Social reward: agents derive utility from improving others' emotional state.
# In v2 the module appraises the partner's valence itself (population lexicon) and no longer switches on the
# emotion module, so this condition changes one pressure only. Add `modules.emotion: true` to combine them;
# social reward then uses emotion's valence.
run_name: social_reward
modules:
  social_reward: true
