# Perfect memory: no encoding loss (verbatim storage, so no memory lens or merging), minimal forgetting,
# deterministic GA top-k retrieval.
run_name: perfect_memory
memory:
  encoding_noise: 0.0
  decay_rate: 0.01
  memory_capacity: 100000
  entity_generalization: 0.0
retrieval:
  temperature: 0.0
